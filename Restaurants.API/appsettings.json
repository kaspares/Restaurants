{ // Specify configuration details for different enviroments in our web API 
  // Example: If we had a development acceptance and a production environment hosted somewhere on the Azure, 
  // we could specify different connection string to the databases inside of this appsettings.json 
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
